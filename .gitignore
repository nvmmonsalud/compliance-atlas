__pycache__/
*.pyc
.DS_Store
.env
*.log
# Local virtualenv
.venv/
# Full internal feed is gitignored; ship the curated output only
data/regulatory-feed.jsonl
# Ship the curated output and the sample feed (regenerated by curate.py)
# data/public/  ← KEEP this shipped
public/reel/  # HyperFrames dev-loop build artifacts, never ship
